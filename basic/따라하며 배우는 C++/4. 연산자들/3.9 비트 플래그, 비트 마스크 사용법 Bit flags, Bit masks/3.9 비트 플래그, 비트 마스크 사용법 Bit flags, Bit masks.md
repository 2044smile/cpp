# 3.9 비트 플래그, 비트 마스크 사용법 Bit flags, Bit masks

## 복습

```cpp
const unsigned char opt0 = 1 << 0;  // << left shift 즉 비트 단위로 왼쪽으로 이동, 0이기 때문에 이동하지 않는다
cout << bitset<8>(opt0) << endl;    // 00000001
```