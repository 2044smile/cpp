# 3.6 논리 연산자 logical operators

## 정수는 0이 아니면 True

## logical AND 가 logical OR 보다 우선순위가 높다

> 괄호를 쳐서 연산하는 걸 추천

```cpp
bool v1 = true;
bool v2 = false;
bool v3 = false;

bool r1 = v1 || v2 && v3;       // true
bool r2 = (v1 || v2 ) && v3;    // false
bool r3 = v1 || (v2 && v3);     // true
```
