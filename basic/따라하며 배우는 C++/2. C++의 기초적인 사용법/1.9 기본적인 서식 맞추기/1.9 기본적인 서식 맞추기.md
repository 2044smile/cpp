# 1.9 기본적인 서식 맞추기

## 최소화, 개발하는 사람들과 맞춰라

## 붙여서 작성하지 말라

```cpp
// [X]
int add(int x, int y) {return x+y;}
// [O]
int add(int x, int y) {return x + y;}

// [X]
int sub(int x, int y) { 
return x - y; 
}
// [O]
int sub(int x, int y) { 
return x - y; 
}
```

## 하드 코딩

- 프로그래머가 코드에서 변수값을 리터럴 같이 고정된 값으로 직접 대입해주는 방식

## 소프트 코딩

- 프로그램 실행 중에 사용자의 입력이나 외부 파일, 인터넷 통신 등으로 데이터를 가져오는 방식
